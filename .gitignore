# 依赖与构建产物
node_modules/
dist/
build/
out/
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# 临时/调试产出 —— 09-15 起**统一放仓库外面**
# （作者本机是 E:\Debug_svStudio；具体路径看配置里的 debugDir，见 main.js）。
# 仓库里一个都不留，这里只是兜一道底。
_debug/
_logs/
效果debug/
*.tmp
svstudio_启动日志.txt
体检结果.txt
